apiVersion: apps/v1
kind: Deployment
metadata:
  name: workout-service
  namespace: default
  labels:
    app: workout-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workout-service
  template:
    metadata:
      labels:
        app: workout-service
    spec:
      containers:
      - name: workout-service
        image: aravindacr.azurecr.io/fitbuddy-workout-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5004
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 5
          periodSeconds: 10
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: bodygpt-config
              key: WORKOUT_SERVICE_PORT
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: bodygpt-secrets
              key: MONGO_URI
        - name: JWT_ACCESS_SECRET
          valueFrom:
            secretKeyRef:
              name: bodygpt-secrets
              key: JWT_ACCESS_SECRET
        - name: AZURE_AI_FOUNDRY_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: bodygpt-secrets
              key: AZURE_AI_FOUNDRY_ENDPOINT
        - name: AZURE_OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: bodygpt-secrets
              key: AZURE_OPENAI_API_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: workout-service
  namespace: default
spec:
  type: ClusterIP
  ports:
  - port: 5004
    targetPort: 5004
    protocol: TCP
  selector:
    app: workout-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: workout-service-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: workout-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
